import type { NextPage } from "next";
import AppLayout from "../../layouts/AppLayout";

const Loans: NextPage = () => {
  return <AppLayout>{({}) => <h1>Loans</h1>}</AppLayout>;
};

export default Loans;
